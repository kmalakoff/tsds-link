# tsds-link

Internal command used by `ts-dev-stack` to link the current package into its
configured `node_modules` location.

Install and configure `ts-dev-stack` to use this command. For setup and usage,
see the [ts-dev-stack documentation](https://www.npmjs.com/package/ts-dev-stack).
